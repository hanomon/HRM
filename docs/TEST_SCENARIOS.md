# 🧪 테스트 시나리오

> **목적**: NFC 기반 근태관리 시스템의 전체 기능 검증  
> **최종 업데이트**: 2026-01-06

## 📋 목차
- [테스트 환경 준비](#-테스트-환경-준비)
- [시나리오 1: 직원 등록 및 NFC 카드 연동](#-시나리오-1-직원-등록-및-nfc-카드-연동)
- [시나리오 2: NFC 태깅으로 출퇴근 체크](#-시나리오-2-nfc-태깅으로-출퇴근-체크)
- [시나리오 3: 출퇴근 기록 조회 및 관리](#-시나리오-3-출퇴근-기록-조회-및-관리)
- [시나리오 4: Excel 내보내기](#-시나리오-4-excel-내보내기)
- [시나리오 5: 직원 정보 수정 및 삭제](#-시나리오-5-직원-정보-수정-및-삭제)
- [시나리오 6: 예외 상황 처리](#-시나리오-6-예외-상황-처리)
- [API 테스트 시나리오](#-api-테스트-시나리오)

---

## 🔧 테스트 환경 준비

### 1. 로컬 개발 환경

```bash
# 1. 저장소 클론
git clone https://github.com/hanomon/HRM.git
cd HRM

# 2. 의존성 설치
npm run install:all

# 3. 서버 실행
npm run dev

# 4. 접속
# Frontend: http://localhost:5173
# Backend: http://localhost:3000/api
```

### 2. 라이브 데모 환경

```
Frontend: https://hrm-frontend-3tph.onrender.com
Backend API: https://hrm-backend-1dk5.onrender.com/api
```

### 3. 테스트 데이터 생성

**방법 1: 명령어 (로컬)**
```bash
cd backend
npm run seed -- --force
```

**방법 2: API 호출 (로컬/라이브)**
```javascript
// 브라우저 콘솔(F12)에서 실행
fetch('https://hrm-backend-1dk5.onrender.com/api/seed', {
  method: 'POST'
})
.then(res => res.json())
.then(data => console.log('✅ 테스트 데이터 생성:', data));
```

**생성되는 테스트 데이터:**
- 직원 3명 (김철수, 이영희, 박민수)
- 최근 7일간 출퇴근 기록 45건

---

## 📝 시나리오 1: 직원 등록 및 NFC 카드 연동

### 목표
새로운 직원을 시스템에 등록하고 NFC 카드를 연동한다.

### 사전 조건
- Frontend 접속 완료
- 관리자 권한 (현재는 인증 없음)

### 테스트 단계

#### Step 1.1: 직원 관리 페이지 접속

1. Frontend 접속
   ```
   로컬: http://localhost:5173
   라이브: https://hrm-frontend-3tph.onrender.com
   ```

2. 상단 네비게이션에서 **"직원 관리"** 클릭

**예상 결과:**
- ✅ 직원 목록 페이지 표시
- ✅ 등록된 직원 리스트 표시 (없으면 빈 목록)
- ✅ "직원 추가" 버튼 표시

---

#### Step 1.2: NFC 카드 ID 확인

**방법 1: NFC 스캔 페이지 이용**

1. 상단 네비게이션에서 **"NFC 태깅"** 클릭
2. **"NFC 스캔 시작"** 버튼 클릭
3. NFC 카드를 태블릿에 가까이 대기
4. 화면에 표시된 NFC ID 복사
   - 예: `04:A1:B2:C3:D4:E5:F6`

**방법 2: 브라우저 콘솔 이용 (개발자)**

```javascript
const ndef = new NDEFReader();
await ndef.scan();
ndef.addEventListener('reading', ({ serialNumber }) => {
  const nfcId = serialNumber.toUpperCase().match(/.{1,2}/g).join(':');
  console.log('📡 NFC ID:', nfcId);
  alert(`NFC ID: ${nfcId}`);
});
```

**예상 결과:**
- ✅ NFC ID가 콜론(:)으로 구분된 형식으로 표시
- ✅ 예: `04:A1:B2:C3:D4:E5:F6`

---

#### Step 1.3: 직원 정보 입력

1. "직원 관리" 페이지에서 **"직원 추가"** 버튼 클릭

2. 폼에 정보 입력:
   ```
   이름: 홍길동
   NFC ID: 04:A1:B2:C3:D4:E5:F6
   부서: 개발팀
   직책: 사원
   이메일: hong@company.com
   전화번호: 010-1234-5678
   ```

3. **"저장"** 버튼 클릭

**예상 결과:**
- ✅ 성공 메시지 표시
- ✅ 직원 목록에 새 직원 추가됨
- ✅ 폼이 초기화되거나 목록으로 돌아감

---

#### Step 1.4: 등록 확인

1. 직원 목록에서 방금 등록한 직원 찾기
2. 정보가 올바르게 표시되는지 확인

**체크리스트:**
- [ ] 이름이 올바르게 표시됨
- [ ] NFC ID가 올바르게 표시됨
- [ ] 부서, 직책이 올바르게 표시됨
- [ ] 이메일, 전화번호가 올바르게 표시됨

**예상 결과:**
- ✅ 입력한 모든 정보가 정확하게 표시됨

---

#### Step 1.5: API 레벨 확인 (개발자용)

```javascript
// 브라우저 콘솔에서 확인
fetch('http://localhost:3000/api/employees')
  .then(res => res.json())
  .then(employees => {
    const newEmployee = employees.find(e => e.name === '홍길동');
    console.log('✅ 등록된 직원:', newEmployee);
  });
```

**예상 결과:**
```json
{
  "id": 4,
  "nfc_id": "04:A1:B2:C3:D4:E5:F6",
  "name": "홍길동",
  "department": "개발팀",
  "position": "사원",
  "email": "hong@company.com",
  "phone": "010-1234-5678",
  "created_at": "2026-01-06T09:30:00.000Z",
  "updated_at": "2026-01-06T09:30:00.000Z"
}
```

---

## 🏷️ 시나리오 2: NFC 태깅으로 출퇴근 체크

### 목표
등록된 직원이 NFC 카드를 태깅하여 출근/퇴근을 기록한다.

### 사전 조건
- 직원이 시스템에 등록되어 있음
- Android 태블릿 (NFC 지원)
- Chrome/Edge 브라우저

---

### 2A. 출근 태깅 (첫 번째 태깅)

#### Step 2A.1: NFC 태깅 페이지 접속

1. Android 태블릿에서 Frontend 접속
2. 상단 네비게이션에서 **"NFC 태깅"** 클릭

**예상 결과:**
- ✅ NFC 태깅 페이지 표시
- ✅ "NFC 스캔 시작" 버튼 표시

---

#### Step 2A.2: NFC 스캔 시작

1. **"NFC 스캔 시작"** 버튼 클릭
2. NFC 권한 요청 팝업이 나타나면 **"허용"** 클릭

**예상 결과:**
- ✅ "NFC 스캔 중..." 메시지 표시
- ✅ 버튼이 "NFC 스캔 중지"로 변경됨
- ✅ 스캔 대기 상태

---

#### Step 2A.3: 출근 태깅

1. 직원(홍길동)이 NFC 카드를 태블릿에 가까이 대기
2. 태깅 소리 또는 진동 (기기 설정에 따라)

**예상 결과:**
- ✅ 화면에 성공 메시지 표시
  ```
  ✅ 홍길동님
  출근 처리되었습니다.
  시간: 09:00:00
  ```
- ✅ 직원 이름, 출퇴근 구분, 시간 표시
- ✅ 녹색 배경 또는 성공 애니메이션

---

#### Step 2A.4: 대시보드에서 확인

1. 상단 네비게이션에서 **"대시보드"** 클릭
2. 오늘 날짜의 출근 기록 확인

**체크리스트:**
- [ ] 홍길동의 출근 기록이 표시됨
- [ ] 출근 시간이 올바르게 표시됨
- [ ] "출근" 상태로 표시됨
- [ ] 퇴근 시간은 비어있음

---

### 2B. 퇴근 태깅 (두 번째 태깅)

#### Step 2B.1: 퇴근 태깅

1. NFC 태깅 페이지로 다시 이동 (스캔이 계속 실행 중)
2. 동일한 직원(홍길동)이 NFC 카드를 다시 태깅

**예상 결과:**
- ✅ 화면에 성공 메시지 표시
  ```
  ✅ 홍길동님
  퇴근 처리되었습니다.
  시간: 18:00:00
  ```
- ✅ "퇴근"으로 구분됨 (자동 판단)

---

#### Step 2B.2: 기록 확인

1. 대시보드에서 오늘 날짜의 기록 확인

**체크리스트:**
- [ ] 출근 시간과 퇴근 시간 모두 표시됨
- [ ] 근무 시간이 계산되어 표시됨 (예: 9시간)
- [ ] 출퇴근 상태가 "완료"로 표시됨

---

### 2C. 연속 태깅 테스트

#### Step 2C.1: 여러 직원 연속 태깅

**테스트 직원:**
1. 김철수 (`04:A1:B2:C3:D4:E5:F6`) - 08:45 출근
2. 이영희 (`04:B2:C3:D4:E5:F6:A1`) - 08:50 출근
3. 박민수 (`04:C3:D4:E5:F6:A1:B2`) - 08:55 출근

**예상 결과:**
- ✅ 각 태깅마다 올바른 직원 이름 표시
- ✅ 모두 "출근" 처리됨
- ✅ 대시보드에 3명의 출근 기록 표시

---

### 2D. API 테스트 (NFC 카드 없이)

NFC 카드가 없어도 API를 통해 테스트할 수 있습니다.

```javascript
// 브라우저 콘솔에서 실행

// 1. 홍길동 출근
fetch('http://localhost:3000/api/attendance', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ nfc_id: '04:A1:B2:C3:D4:E5:F6' })
})
.then(res => res.json())
.then(data => console.log('✅ 출근:', data));

// 2. 5초 후 홍길동 퇴근
setTimeout(() => {
  fetch('http://localhost:3000/api/attendance', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ nfc_id: '04:A1:B2:C3:D4:E5:F6' })
  })
  .then(res => res.json())
  .then(data => console.log('✅ 퇴근:', data));
}, 5000);
```

---

## 📊 시나리오 3: 출퇴근 기록 조회 및 관리

### 목표
특정 기간의 출퇴근 기록을 조회하고 관리한다.

---

#### Step 3.1: 전체 기록 조회

1. 상단 네비게이션에서 **"대시보드"** 또는 **"출퇴근 기록"** 클릭
2. 전체 출퇴근 기록 목록 확인

**예상 결과:**
- ✅ 출퇴근 기록 테이블 표시
- ✅ 컬럼: 직원명, 부서, 출근시간, 퇴근시간, 근무시간, 날짜
- ✅ 최신 기록이 위에 표시 (내림차순)

---

#### Step 3.2: 날짜 필터링

1. 시작 날짜 선택: `2026-01-01`
2. 종료 날짜 선택: `2026-01-06`
3. "조회" 버튼 클릭

**예상 결과:**
- ✅ 선택한 기간의 기록만 표시됨
- ✅ 기간 외 기록은 숨겨짐

---

#### Step 3.3: 직원별 필터링 (선택사항)

1. 직원 선택 드롭다운: "홍길동" 선택
2. 필터 적용

**예상 결과:**
- ✅ 홍길동의 기록만 표시됨
- ✅ 다른 직원 기록은 숨겨짐

---

#### Step 3.4: 기록 상세 보기

1. 특정 출퇴근 기록 클릭
2. 상세 정보 모달 또는 페이지 표시

**예상 결과:**
- ✅ 직원 정보 표시
- ✅ 출근/퇴근 시간 상세 표시
- ✅ 근무 시간 계산 표시
- ✅ NFC ID 표시

---

#### Step 3.5: 잘못된 기록 삭제

1. 삭제할 기록의 "삭제" 버튼 클릭
2. 확인 대화상자에서 "확인" 클릭

**예상 결과:**
- ✅ 확인 메시지: "이 기록을 삭제하시겠습니까?"
- ✅ 삭제 후 목록에서 제거됨
- ✅ 성공 메시지 표시

---

#### Step 3.6: API로 기록 조회 (개발자용)

```javascript
// 전체 기록 조회
fetch('http://localhost:3000/api/attendance')
  .then(res => res.json())
  .then(records => console.log('✅ 전체 기록:', records));

// 날짜 필터링
fetch('http://localhost:3000/api/attendance?start_date=2026-01-01&end_date=2026-01-06')
  .then(res => res.json())
  .then(records => console.log('✅ 필터링된 기록:', records));
```

---

## 📥 시나리오 4: Excel 내보내기

### 목표
출퇴근 기록을 Excel 파일로 다운로드한다.

---

#### Step 4.1: Excel 내보내기 버튼 클릭

1. 출퇴근 기록 페이지에서
2. **"Excel 내보내기"** 버튼 클릭

**예상 결과:**
- ✅ Excel 파일 자동 다운로드
- ✅ 파일명: `attendance_YYYY-MM-DD.xlsx`

---

#### Step 4.2: Excel 파일 확인

1. 다운로드된 Excel 파일 열기
2. 내용 확인

**예상 컬럼:**
| 직원명 | 부서 | 직책 | 출근시간 | 퇴근시간 | 근무시간 | 날짜 |
|--------|------|------|----------|----------|----------|------|
| 홍길동 | 개발팀 | 사원 | 09:00 | 18:00 | 9시간 | 2026-01-06 |
| 김철수 | 개발팀 | 팀장 | 08:45 | 18:30 | 9시간 45분 | 2026-01-06 |

**체크리스트:**
- [ ] 모든 기록이 포함됨
- [ ] 한글이 정상적으로 표시됨
- [ ] 시간 형식이 올바름
- [ ] 근무 시간이 계산되어 있음

---

#### Step 4.3: 날짜 필터 후 Excel 내보내기

1. 날짜 필터 적용 (예: 2026-01-01 ~ 2026-01-06)
2. "Excel 내보내기" 버튼 클릭

**예상 결과:**
- ✅ 필터링된 기록만 포함된 Excel 파일 다운로드
- ✅ 파일명에 날짜 범위 포함

---

#### Step 4.4: API로 Excel 다운로드 (개발자용)

```javascript
// 브라우저에서 실행
window.open('http://localhost:3000/api/attendance/export/excel?start_date=2026-01-01&end_date=2026-01-06');

// 또는 fetch로
fetch('http://localhost:3000/api/attendance/export/excel')
  .then(res => res.blob())
  .then(blob => {
    const url = window.URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = 'attendance.xlsx';
    a.click();
  });
```

---

## ✏️ 시나리오 5: 직원 정보 수정 및 삭제

### 목표
등록된 직원의 정보를 수정하거나 삭제한다.

---

### 5A. 직원 정보 수정

#### Step 5A.1: 수정할 직원 선택

1. "직원 관리" 페이지 접속
2. 홍길동 직원의 "수정" 버튼 클릭

**예상 결과:**
- ✅ 수정 폼 표시
- ✅ 기존 정보가 폼에 채워져 있음

---

#### Step 5A.2: 정보 수정

```
기존:
부서: 개발팀
직책: 사원

변경:
부서: 개발팀
직책: 주임
```

1. 직책을 "주임"으로 변경
2. "저장" 버튼 클릭

**예상 결과:**
- ✅ 성공 메시지 표시
- ✅ 직원 목록에 변경사항 반영됨
- ✅ 직책이 "주임"으로 표시됨

---

#### Step 5A.3: 수정 확인

1. 직원 목록에서 홍길동 직원 확인
2. 직책이 "주임"으로 변경되었는지 확인

**API 확인:**
```javascript
fetch('http://localhost:3000/api/employees/4')
  .then(res => res.json())
  .then(employee => {
    console.log('✅ 수정된 직원:', employee);
    console.log('직책:', employee.position); // "주임"
  });
```

---

### 5B. 직원 삭제

#### Step 5B.1: 테스트용 직원 추가

```
이름: 테스트직원
NFC ID: 04:FF:FF:FF:FF:FF:FF
부서: 테스트팀
직책: 임시
```

---

#### Step 5B.2: 직원 삭제

1. 테스트직원의 "삭제" 버튼 클릭
2. 확인 대화상자: "이 직원을 삭제하시겠습니까?" → "확인"

**예상 결과:**
- ✅ 확인 메시지 표시
- ✅ 삭제 후 목록에서 제거됨
- ✅ 성공 메시지: "직원이 삭제되었습니다."

---

#### Step 5B.3: 삭제 확인

**체크리스트:**
- [ ] 직원 목록에서 사라짐
- [ ] 해당 직원의 출퇴근 기록도 함께 삭제됨 (CASCADE)

**API 확인:**
```javascript
// 삭제된 직원 조회 시도 (404 예상)
fetch('http://localhost:3000/api/employees/5')
  .then(res => {
    if (res.status === 404) {
      console.log('✅ 직원이 삭제되었습니다.');
    }
  });
```

---

## ⚠️ 시나리오 6: 예외 상황 처리

### 목표
시스템이 예외 상황을 올바르게 처리하는지 확인한다.

---

### 6A. 미등록 NFC 카드 태깅

#### Step 6A.1: 미등록 카드 태깅

**시뮬레이션:**
```javascript
fetch('http://localhost:3000/api/attendance', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ nfc_id: '99:99:99:99:99:99:99' })
})
.then(res => res.json())
.then(data => console.log('응답:', data));
```

**예상 결과:**
```json
{
  "error": "등록되지 않은 NFC 카드입니다."
}
```

- ✅ 에러 메시지 표시
- ✅ 빨간색 배경 또는 경고 애니메이션
- ✅ 기록이 생성되지 않음

---

### 6B. 중복 NFC ID 등록 시도

#### Step 6B.1: 이미 등록된 NFC ID로 직원 추가

```
이름: 중복테스트
NFC ID: 04:A1:B2:C3:D4:E5:F6 (홍길동과 동일)
부서: 테스트팀
직책: 사원
```

**예상 결과:**
- ✅ 에러 메시지: "이미 등록된 NFC ID입니다."
- ✅ 직원이 추가되지 않음
- ✅ 폼이 초기화되지 않음 (재입력 가능)

---

### 6C. 필수 입력 누락

#### Step 6C.1: 이름 없이 직원 추가 시도

```
이름: (비어있음)
NFC ID: 04:AA:BB:CC:DD:EE:FF
부서: 개발팀
직책: 사원
```

**예상 결과:**
- ✅ 에러 메시지: "이름은 필수 항목입니다."
- ✅ 해당 필드에 빨간색 테두리
- ✅ 저장되지 않음

---

### 6D. 잘못된 NFC ID 형식

#### Step 6D.1: 형식이 잘못된 NFC ID 입력

```
잘못된 형식:
- "123456"
- "ABCD"
- "04-A1-B2-C3-D4-E5-F6" (하이픈 사용)
```

**예상 결과:**
- ✅ 에러 메시지: "올바른 NFC ID 형식이 아닙니다."
- ✅ 예시 표시: "04:A1:B2:C3:D4:E5:F6"
- ✅ 저장되지 않음

---

### 6E. 네트워크 오류

#### Step 6E.1: Backend 서버 중단 후 태깅

1. Backend 서버 중지 (`Ctrl+C`)
2. Frontend에서 NFC 태깅 시도

**예상 결과:**
- ✅ 에러 메시지: "서버와 연결할 수 없습니다."
- ✅ 재시도 안내 메시지
- ✅ 로딩 상태 해제

---

### 6F. 동시 태깅

#### Step 6F.1: 여러 명이 동시에 태깅

**시뮬레이션:**
```javascript
// 동시에 3명 태깅
Promise.all([
  fetch('http://localhost:3000/api/attendance', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ nfc_id: '04:A1:B2:C3:D4:E5:F6' })
  }),
  fetch('http://localhost:3000/api/attendance', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ nfc_id: '04:B2:C3:D4:E5:F6:A1' })
  }),
  fetch('http://localhost:3000/api/attendance', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ nfc_id: '04:C3:D4:E5:F6:A1:B2' })
  })
])
.then(responses => Promise.all(responses.map(r => r.json())))
.then(results => console.log('✅ 동시 태깅 결과:', results));
```

**예상 결과:**
- ✅ 모든 태깅이 정상 처리됨
- ✅ 각각 올바른 직원으로 기록됨
- ✅ 시간 순서대로 기록됨

---

## 🔌 API 테스트 시나리오

### API 1: 직원 관리

#### 1.1 전체 직원 조회
```bash
curl -X GET http://localhost:3000/api/employees
```

**예상 응답:**
```json
[
  {
    "id": 1,
    "nfc_id": "04:A1:B2:C3:D4:E5:F6",
    "name": "김철수",
    "department": "개발팀",
    "position": "팀장",
    "email": "kim@company.com",
    "phone": "010-1234-5678"
  }
]
```

#### 1.2 직원 추가
```bash
curl -X POST http://localhost:3000/api/employees \
  -H "Content-Type: application/json" \
  -d '{
    "nfc_id": "04:11:22:33:44:55:66",
    "name": "최영수",
    "department": "영업팀",
    "position": "대리",
    "email": "choi@company.com",
    "phone": "010-9999-8888"
  }'
```

#### 1.3 NFC ID로 직원 조회
```bash
curl -X GET "http://localhost:3000/api/employees/nfc/04:A1:B2:C3:D4:E5:F6"
```

#### 1.4 직원 수정
```bash
curl -X PUT http://localhost:3000/api/employees/1 \
  -H "Content-Type: application/json" \
  -d '{
    "position": "부장"
  }'
```

#### 1.5 직원 삭제
```bash
curl -X DELETE http://localhost:3000/api/employees/1
```

---

### API 2: 출퇴근 기록

#### 2.1 출퇴근 태깅
```bash
curl -X POST http://localhost:3000/api/attendance \
  -H "Content-Type: application/json" \
  -d '{
    "nfc_id": "04:A1:B2:C3:D4:E5:F6"
  }'
```

**첫 번째 태깅 응답 (출근):**
```json
{
  "id": 1,
  "employee_id": 1,
  "employee_name": "김철수",
  "nfc_id": "04:A1:B2:C3:D4:E5:F6",
  "tag_type": "check_in",
  "tag_time": "2026-01-06T08:45:00.000Z",
  "message": "출근 처리되었습니다."
}
```

**두 번째 태깅 응답 (퇴근):**
```json
{
  "id": 2,
  "employee_id": 1,
  "employee_name": "김철수",
  "nfc_id": "04:A1:B2:C3:D4:E5:F6",
  "tag_type": "check_out",
  "tag_time": "2026-01-06T18:30:00.000Z",
  "message": "퇴근 처리되었습니다."
}
```

#### 2.2 기록 조회
```bash
# 전체 조회
curl -X GET http://localhost:3000/api/attendance

# 날짜 필터
curl -X GET "http://localhost:3000/api/attendance?start_date=2026-01-01&end_date=2026-01-06"
```

#### 2.3 Excel 내보내기
```bash
curl -X GET "http://localhost:3000/api/attendance/export/excel?start_date=2026-01-01&end_date=2026-01-06" \
  -o attendance.xlsx
```

#### 2.4 기록 삭제
```bash
curl -X DELETE http://localhost:3000/api/attendance/1
```

---

### API 3: 테스트 데이터

#### 3.1 Seed 데이터 생성
```bash
curl -X POST http://localhost:3000/api/seed
```

**응답:**
```json
{
  "success": true,
  "message": "테스트 데이터가 생성되었습니다!",
  "data": {
    "employees": 3,
    "checkIn": 24,
    "checkOut": 21,
    "total": 45
  }
}
```

---

### API 4: 헬스 체크

#### 4.1 서버 상태 확인
```bash
curl -X GET http://localhost:3000/api/health
```

**응답:**
```json
{
  "status": "ok",
  "message": "서버가 정상 작동중입니다."
}
```

---

## 📋 테스트 체크리스트

### 전체 기능 테스트

- [ ] **직원 등록**
  - [ ] 정상 등록
  - [ ] 중복 NFC ID 차단
  - [ ] 필수 입력 검증
  - [ ] NFC ID 형식 검증

- [ ] **NFC 태깅**
  - [ ] 출근 태깅
  - [ ] 퇴근 태깅
  - [ ] 연속 태깅
  - [ ] 미등록 카드 처리

- [ ] **기록 조회**
  - [ ] 전체 조회
  - [ ] 날짜 필터
  - [ ] 직원별 필터
  - [ ] 상세 보기

- [ ] **Excel 내보내기**
  - [ ] 전체 기록 내보내기
  - [ ] 필터링 후 내보내기
  - [ ] 파일 형식 확인
  - [ ] 한글 깨짐 없음

- [ ] **직원 관리**
  - [ ] 정보 수정
  - [ ] 직원 삭제
  - [ ] 삭제 시 출퇴근 기록도 삭제

- [ ] **예외 처리**
  - [ ] 미등록 카드 에러
  - [ ] 네트워크 에러
  - [ ] 입력 검증 에러

---

## 🎯 성능 테스트

### 대량 데이터 테스트

```javascript
// 100명의 직원 추가
for (let i = 1; i <= 100; i++) {
  await fetch('http://localhost:3000/api/employees', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      nfc_id: `04:AA:BB:CC:DD:EE:${i.toString(16).padStart(2, '0')}`,
      name: `테스트직원${i}`,
      department: '테스트팀',
      position: '사원',
      email: `test${i}@company.com`,
      phone: `010-0000-${i.toString().padStart(4, '0')}`
    })
  });
}

console.log('✅ 100명 직원 등록 완료');
```

**확인 사항:**
- [ ] 직원 목록 로딩 속도
- [ ] 검색/필터링 속도
- [ ] Excel 내보내기 속도

---

## 📞 문제 발생 시

### 1. 로그 확인
```bash
# Backend 로그
npm run dev:backend

# Frontend 로그
브라우저 F12 → Console 탭
```

### 2. 데이터베이스 초기화
```bash
cd backend
rm attendance.db
npm run dev
npm run seed -- --force
```

### 3. 이슈 등록
GitHub Issues: https://github.com/hanomon/HRM/issues

---

**작성**: HRM 개발팀  
**최종 업데이트**: 2026-01-06  
**버전**: 1.0.0

